# Host Integration

The gateway contract is identical across:

- Codex CLI / Desktop
- Claude Code
- Cursor
- OpenCode

Reviewable examples live under `templates/codegraph/`.

The gateway should be configured as a local MCP server. Raw CodeGraph should not be exposed directly to the subject agent.
