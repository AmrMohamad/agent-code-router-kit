# Claude Instruction Example

Before searching a Swift/iOS codebase, classify the task.

Known Swift symbols require semantic navigation. High-fanout symbols require grouped counts before reference expansion. Literal/resource/generated surfaces start with search. Syntax-shaped Swift migrations start with ast-grep. Build and runtime claims require the project proof layer.

