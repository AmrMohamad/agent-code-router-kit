# Cursor Rule Example

For Swift/iOS codebase work:

- Use semantic navigation for known Swift symbols.
- Use grouped counts before expanding high-fanout references.
- Use search for literals, resources, generated files, and broad inventory.
- Use ast-grep for syntax-shaped Swift patterns.
- Use the project build/test/runtime layer for correctness proof.

