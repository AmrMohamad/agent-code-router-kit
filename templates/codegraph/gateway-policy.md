# CodeGraph Gateway Policy

The gateway is discovery-only.

- Prefer Serena/LSP for exact symbol proof.
- Prefer `rg` / `fd` for literals and resources.
- Prefer `ast-grep` for syntax-shaped work.
- Prefer runtime tools for build, test, crash, simulator, and device claims.
- Use the gateway sparingly and stop after a coherent bounded result.
