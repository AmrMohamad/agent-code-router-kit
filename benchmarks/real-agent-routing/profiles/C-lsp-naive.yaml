profile_id: C-lsp-naive
display_name: LSP-enabled naive
allowed_tools:
  - Serena
  - Kotlin LSP
  - Android Studio semantic declaration/usages
  - rg
  - fd
blocked_tools: []
required_first_tool: semantic_for_known_symbol
high_fanout_policy: raw_search_allowed
max_raw_output_bytes: 50000
instructions: |
  Semantic tools are available for known symbols. Routing discipline remains off:
  use the same controlled raw high-fanout policy as A-search-only so A/C isolates
  semantic access without adding summary-first behavior.
